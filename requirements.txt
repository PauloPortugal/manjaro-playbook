# Python dependencies for manjaro-playbook development and testing
#
# IMPORTANT FOR MANJARO/ARCH USERS:
# Due to PEP 668 (externally-managed Python environments), install via pacman instead:
#   sudo pacman -S ansible ansible-lint yamllint python-pytokens
#
# For other distributions, use: pip install -r requirements.txt
#
# Or use the automated setup script: ./setup-dev-environment.sh

# Core Ansible
ansible>=2.9,<3.0
ansible-core>=2.12

# Linting and Quality Tools
ansible-lint>=6.0.0
yamllint>=1.30.0

# Dependencies for ansible-lint (python-pytokens on Arch/Manjaro)
pytokens>=1.8.0

# Testing (optional, for future Molecule integration)
# molecule>=5.0.0
# molecule-plugins[docker]>=23.0.0

---
- name: Update cache and upgrade
  community.general.pacman:
    update_cache: true
    upgrade: true
    upgrade_extra_args: --noconfirm
  become: true
  when: not (base_role_complete | default(false))
  tags:
    - pre

- name: Install Utils
  community.general.pacman:
    name: "{{ utils }}"
    state: present
  become: true
  when: not (base_role_complete | default(false))
  tags:
    - utils
    - base

- name: Install Utils from Arch User Repository
  ansible.builtin.shell: |
    set -o pipefail
    pamac build -d --no-confirm {{ item }} | grep "is up to date"
    if [ $? != 0 ]; then
      pamac build --no-confirm {{ item }}
    fi
  loop: "{{ utils_aur }}"
  register: command_result
  failed_when: command_result.rc != 0
  changed_when: "'successfully' in command_result.stdout"
  when: not (base_role_complete | default(false))
  tags:
    - base
    - utils-aur
    - aur

- name: Configure NTP
  ansible.builtin.import_tasks: ntp.yml
  when: not (base_role_complete | default(false))

- name: Configure PIP
  ansible.builtin.import_tasks: pip.yml
  when: not (base_role_complete | default(false))

- name: Configure Bash
  ansible.builtin.import_tasks: bash.yml
  when: not (base_role_complete | default(false))

- name: Configure Zsh
  ansible.builtin.import_tasks: zsh.yml
  when: not (base_role_complete | default(false))

- name: Configure terminator
  ansible.builtin.import_tasks: terminator.yml
  when: not (base_role_complete | default(false))

- name: Creates Workspace directory
  ansible.builtin.file:
    path: /home/{{ user_name }}/Workspace
    state: directory
    mode: "0755"
  become_user: "{{ user_name }}"
  become: true
  when: not (base_role_complete | default(false))

- name: Mark base role as complete
  ansible.builtin.set_fact:
    base_role_complete: true
  tags:
    - always
